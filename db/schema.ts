import{index,integer,sqliteTable,text,uniqueIndex}from"drizzle-orm/sqlite-core";
export const organizations=sqliteTable("organizations",{id:text("id").primaryKey(),name:text("name").notNull(),createdAt:integer("created_at").notNull()});
export const projects=sqliteTable("projects",{id:text("id").primaryKey(),organizationId:text("organization_id").notNull(),name:text("name").notNull(),ownerUserId:text("owner_user_id").notNull(),createdAt:integer("created_at").notNull()},t=>[index("idx_projects_org").on(t.organizationId)]);
export const members=sqliteTable("project_members",{id:text("id").primaryKey(),projectId:text("project_id").notNull(),userId:text("user_id").notNull(),email:text("email").notNull(),role:text("role",{enum:["owner","editor","viewer"]}).notNull()},t=>[uniqueIndex("idx_members_project_user").on(t.projectId,t.userId)]);
export const versions=sqliteTable("project_versions",{id:text("id").primaryKey(),projectId:text("project_id").notNull(),fileName:text("file_name").notNull(),sha256:text("sha256").notNull(),metadataJson:text("metadata_json"),extractionStatus:text("extraction_status").notNull(),extractionReason:text("extraction_reason"),createdAt:integer("created_at").notNull()},t=>[index("idx_versions_project_created").on(t.projectId,t.createdAt)]);
export const issues=sqliteTable("issues",{id:text("id").primaryKey(),projectId:text("project_id").notNull(),versionId:text("version_id"),objectType:text("object_type").notNull(),objectId:text("object_id").notNull(),title:text("title").notNull(),description:text("description"),severity:text("severity").notNull(),category:text("category").notNull(),status:text("status").notNull(),assigneeId:text("assignee_id"),createdBy:text("created_by").notNull(),createdAt:integer("created_at").notNull()},t=>[index("idx_issues_project_status").on(t.projectId,t.status)]);
export const comments=sqliteTable("comments",{id:text("id").primaryKey(),issueId:text("issue_id").notNull(),authorId:text("author_id").notNull(),body:text("body").notNull(),createdAt:integer("created_at").notNull()},t=>[index("idx_comments_issue").on(t.issueId)]);
